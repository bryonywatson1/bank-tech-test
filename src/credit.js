function Credit() {
    this.amount = 0;
}
Credit.prototype.addCredit = function(number) {
  this.amount += number;
  date.dateIs(date);
};

Credit.prototype.addADate = function(date){
  this.timeStamp = date;
  date.dateIs(date);
};
